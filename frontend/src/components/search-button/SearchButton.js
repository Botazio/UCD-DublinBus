import SearchButtonCSS from './SearchButton.module.css';

const SearchButton = () => {
   return (
      <div className={SearchButtonCSS.search_button}>
         <h3>Search</h3>
      </div>
   );
}

export default SearchButton;